# M2B Retrieval Baseline Results

## Status

This is a template baseline for `M2B-0`.

No runtime retriever was executed.
No database session was opened.
No `DataAgentService` path was invoked.

This document does **not** represent actual retriever performance.

## Why Template Only

`M2B-0` is limited to inventory, taxonomy, golden-set definition, and baseline framework setup. Real deterministic baseline execution is intentionally deferred to a later M2B follow-up after the knowledge inputs are governed.

## Template Output

The committed machine-readable baseline file is:

- `data_knowledge_eval/m2b/baseline_results.template.json`

It contains:

- deterministic metadata
- the requested case list
- empty retrieval result buckets
- TODO placeholders for future deterministic or hybrid evaluation

## Next Step

The next stage after `M2B-0` is `M2B-1 Structured Knowledge Extraction`.
